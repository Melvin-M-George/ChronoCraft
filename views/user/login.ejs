<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChronoCraft Login</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="shortcut icon" type="image/x-icon" href="/Images/favicon.ico">
    <style>
        body, html {
            height: 100%;
            margin: 0;
        }

        .login-container {
            display: flex;
            height: 100vh;
        }

        .watch-section {
            flex: 1;
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 0;
            overflow: hidden;
        }

        .watch-image {
            width: 100%;
            height: 100%;
            object-fit: cover; /* This ensures the image covers the entire section */
            object-position: center; /* Centers the image */
        }

        .login-section {
            flex: 1;
            display: flex;
            align-items: center;
            background-color: white;
            padding: 40px;
        }

        .login-wrap {
            background: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            margin: 0 auto;
        }

        /* Rest of the existing CSS remains the same */
        .heading_s1 {
            text-align: center;
            margin-bottom: 30px;
        }

        .heading_s1 h3 {
            font-size: 24px;
            font-weight: 600;
            color: #333;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-control {
            height: 50px;
            border-radius: 5px;
            border: 1px solid #ddd;
            padding: 10px 15px;
            font-size: 14px;
        }

        .login_footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .btn-fill-out {
            background-color: #000000;
            color: white;
            width: 100%;
            height: 50px;
            border: none;
            border-radius: 5px;
            font-weight: 500;
            transition: all 0.3s;
        }

        .btn-fill-out:hover {
            background-color: rgb(48, 48, 48);
            color: white;
        }

        .divider-text-center {
            text-align: center;
            position: relative;
            margin: 25px 0;
        }

        .divider-text-center::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            width: 45%;
            height: 1px;
            background: #ddd;
        }

        .divider-text-center::after {
            content: '';
            position: absolute;
            right: 0;
            top: 50%;
            width: 45%;
            height: 1px;
            background: #ddd;
        }

        .divider-text-center span {
            background: white;
            padding: 0 15px;
            color: #666;
            font-size: 14px;
        }

        .btn-google {
            background-color: #000000;
            color: white;
            width: 100%;
            height: 50px;
            border: none;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            transition: all 0.3s;
            text-decoration: none;
            margin-bottom: 20px;
        }

        .btn-google:hover {
            background-color: rgb(48, 48, 48);
            color: white;
        }

        .text-muted {
            text-align: center;
            font-size: 14px;
            color: #666;
        }

        .text-muted a {
            color: #424874;
            text-decoration: none;
        }

        .text-muted a:hover {
            color: #424874;
        }

        .alert {
            margin-bottom: 20px;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .login-container {
                flex-direction: column;
            }

            .watch-section, .login-section {
                flex: none;
                width: 100%;
                height: auto;
            }

            .watch-image {
                max-height: 50vh;
                object-fit: cover;
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="watch-section">
            <img src="/Images/rado login image.jpg" alt="Luxury Watch" class="watch-image">
        </div>
        <div class="login-section">
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-12">
                        <div class="login-wrap">
                            <div class="heading_s1">
                                <h3>Login</h3>
                                <%if(locals.message && message.length > 0){
                                    %>
                                   <div class="alert alert-danger text-center"> <%= message%></div>
                                    <%
                                }%>
                            </div>
                            
                            <form method="post">
                                <div class="form-group">
                                    <input type="text" class="form-control" required name="email" placeholder="Your Email">
                                </div>
                                <div class="form-group">
                                    <input class="form-control" required type="password" name="password" placeholder="Password">
                                </div>
                                <div class="login_footer form-group">
                                    <a class="text-muted" href="/forgot-password">Forgot password?</a>
                                </div>
                                <div class="form-group">
                                    <button type="submit" class="btn btn-fill-out" name="login">Log in</button>
                                </div>
                                <div class="divider-text-center">
                                    <span>or</span>
                                </div>
                                <a href="/auth/google" class="btn-google">
                                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                                        <path d="M12.545,10.239v3.821h5.445c-0.712,2.315-2.647,3.972-5.445,3.972c-3.332,0-6.033-2.701-6.033-6.032s2.701-6.032,6.033-6.032c1.498,0,2.866,0.549,3.921,1.453l2.814-2.814C17.503,2.988,15.139,2,12.545,2C7.021,2,2.543,6.477,2.543,12s4.478,10,10.002,10c8.396,0,10.249-7.85,9.426-11.748L12.545,10.239z"/>
                                    </svg>
                                    Login With Google
                                </a>
                                <div class="text-muted">
                                    Don't have an account? <a href="/signup">Sign Up now</a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>